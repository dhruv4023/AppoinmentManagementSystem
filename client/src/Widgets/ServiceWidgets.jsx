import FlexBetween from "Components/FlexBetween";
import WidgetWrapper from "Components/WidgetWrapper";
import React from "react";

const ServiceWidgets = ({ userId }) => {
  return <WidgetWrapper>
    helo
  </WidgetWrapper>;
};

export default ServiceWidgets;
